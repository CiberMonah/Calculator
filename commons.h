#include <stdio.h>
#include <stdlib.h>

enum stack_commands {
    push = 1,
    pop = 2,
    add = 3,
    mul = 4,
    div_ = 5,
    sqrt = 6,
    sup = 7,
    sin = 8,
    cos = 9,
    in = 10,
    out = 11,
    hlt = -1
};
