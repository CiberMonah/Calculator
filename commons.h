#ifndef COMMONS_H_INCLUDED
#define COMMONS_H_INCLUDED

#include <stdio.h>
#include <stdlib.h>

enum stack_commands {
    push = 1,
    pop = 2,
    add = 3,
    mul = 4,
    div_ = 5,
    sqrt_ = 6,
    sup = 7,
    sin_ = 8,
    cos_ = 9,
    in = 10,
    out = 11,
    hlt = -1
};

#endif